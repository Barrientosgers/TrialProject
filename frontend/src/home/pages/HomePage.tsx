import React from "react";
import ListingGrid from "../components/ListingGrid";

const HomePage = () => {
  return (
    <React.Fragment>
      <ListingGrid />
    </React.Fragment>
  );
};

export default HomePage;
